schema {#定义查询
    query: UserQuery
}



"用户查询"
type UserQuery{#定义查询类型
    #getUser要和编写的resolver中的方法名保持一致，参数名可以不一样，只要类型一致即可，否则调用报错
    "根据id查询用户"
    getUser(userId:ID):UserVO
}


"用户返回对象"
type UserVO{#定义返回对象
    "用户id"
    userId:ID
    "用户名称"
    userName:String
    age:Int
}



